apiVersion: v1
kind: Service

metadata:
  name: ai-operations-assistant-service

spec:
  selector:
    app: ai-operations-assistant

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: ClusterIP